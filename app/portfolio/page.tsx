export default function Portfolio(){return <main className="page"><header className="page-hero"><p className="eyebrow">SELECTED WORK</p><h1>Stories we<br/><i>love to tell.</i></h1><p>Replace the media placeholders below with OM’s real wedding work. The layout is ready for photos, reels and full cinematic films.</p></header><section className="section portfolio-grid">{Array.from({length:8},(_,i)=><div className={`portfolio-placeholder p${i+1}`} key={i}><span>YOUR PORTFOLIO {String(i+1).padStart(2,"0")}</span></div>)}</section></main>}