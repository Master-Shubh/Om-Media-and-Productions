import Link from "next/link"; import {packages} from "@/lib/data";
export default function Packages(){return <main className="page"><header className="page-hero"><p className="eyebrow">WEDDING COLLECTIONS</p><h1>Packages made<br/><i>for every scale.</i></h1><p>Every wedding is different. Use these collections as a starting point; final quotes are customized to your dates, functions and production requirements.</p></header><section className="section package-list">{packages.map((p,i)=><article className={`package-card large ${i===2?"featured":""}`} key={p.name}><div><span className="package-no">0{i+1}</span><h2>{p.name}</h2><p>{p.note}</p></div><ul>{p.items.map(x=><li key={x}>{x}</li>)}</ul><Link className="btn gold" href="/contact">Request Quote</Link></article>)}</section></main>}