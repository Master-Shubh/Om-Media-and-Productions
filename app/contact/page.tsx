"use client"; import {useState} from "react"; import {services,site,whatsapp} from "@/lib/data";
export default function Contact(){const [sent,setSent]=useState(false);return <main className="page"><header className="page-hero"><p className="eyebrow">CONTACT OM</p><h1>Let's create<br/><i>something timeless.</i></h1><p>Share your event details and our team will get back to you with a tailored production plan.</p></header><section className="section contact-grid"><div className="contact-info"><p><b>PHONE</b><a href={`tel:+91${site.phone}`}>+91 {site.phone}</a></p><p><b>EMAIL</b><a href={`mailto:${site.email}`}>{site.email}</a></p><p><b>LOCATION</b>{site.location}</p><p><b>INSTAGRAM</b>@Om_Media_and_Product</p><a className="btn gold" href={whatsapp} target="_blank" rel="noreferrer">Chat on WhatsApp</a></div><form className="contact-form" onSubmit={e=>{e.preventDefault();setSent(true)}}>{sent?<div className="success"><h2>Thank you.</h2><p>Your enquiry form is ready to connect to your CRM/email backend. For immediate response, use WhatsApp.</p><a className="btn gold" href={whatsapp} target="_blank" rel="noreferrer">Continue on WhatsApp</a></div>:<><input required name="name" placeholder="Full Name *"/><input required name="phone" placeholder="Phone Number *"/><input type="email" name="email" placeholder="Email"/><input type="date" name="date"/><select name="service"><option value="">Select Service</option>{services.map(([t])=><option key={t}>{t}</option>)}</select><textarea name="message" rows={6} placeholder="Tell us about your wedding / event"></textarea><button className="btn gold" type="submit">Request a Quote</button></>}</form></section></main>}